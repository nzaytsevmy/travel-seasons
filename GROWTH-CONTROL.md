# Недельное управление ростом TravelTribe

Этот файл — короткий управляющий слой над редакционными очередями. Он отвечает
на вопрос «что делать на этой неделе», а очереди и профильные промты — «какую
конкретную задачу брать и как её выполнить». Его обновляет задача
`TravelTribe — недельный контролёр роста`.

## Текущее решение

- `updated_at`: **2026-09-04**
- `valid_through`: **2026-09-11**
- `evidence_state`: **MEASURED** — решение опирается на сопоставимые окна 28/56
  дней, но причинных заявлений не делает.
- `publication_gate`: **RUN** — доказательный полезный контент можно выпускать.
- `monetization_decision_gate`: **HOLD** — партнёрская выгрузка не подана,
  поэтому `approved net revenue` равен нулю не по факту, а по отсутствию данных.
  Менять победителя, масштабировать эксперимент или объявлять рост денег нельзя.
  Условие снятия: выгрузка с раздельными approved / pending / reversals, покрытие
  точного join `click_id`/`sub_id` и записанный заранее контракт эксперимента для
  `npm run gate:monetization:decision`.

| Контур | Режим | Приоритет до следующего контроля | Причина |
|---|---|---|---|
| `news` | `KEEP` | Отбор только по событию и новостному рубрикатору | 78 заметок за окно дали 362 показа и 4 клика в Google и 111 органических визитов входа, из них 111 — одна заметка. Ни доказательства роста, ни основания резать |
| `article` | `KEEP` | Верхняя открытая задача `DAILY-ARTICLE-QUEUE.md` — сейчас `WAVE` по `country×month` | Показы 12 055 → 18 074 (+50%), клики 247 → 238 (−4%), позиция 7,88 → 8,60. Растёт охват, не отдача |
| `brand` | `KEEP` | Верхняя достижимая открытая строка `BRAND-QUEUE.md` | Одиннадцать хабов, но в Google видны 2 URL со 177 показами и 2 кликами. Хабам 1–11 дней, окно незрелое |
| `guide_route` | `RUN` + `QUEUE_RECOVERY` | Полностью сверить кандидатов и записать верхнюю прошедшую строку до начала текста | Единственная строка закрыта 03.09. Пустая очередь сама по себе не является основанием для `HOLD`; это устранимый разрыв процесса |

### Управляющие поля для исполнителей

- `news_quota`: **4** — без изменения. Исполняемый конфиг `news/config.json`
  (`targetPerDay: 4`, `maxPerDay: 4`) и контрактный тест закрепляют это число;
  фактический выпуск за окно — 2,8 заметки в день. Поднимать некуда: это потолок.
  Опускать правило не разрешает — условие «ноль при квоте 2 два окна подряд» не
  выполнено. ⛔ Замечание для Никиты: постановка задачи называет умолчанием `2`,
  репозиторий — `4`; расхождение решает человек, контролёр частоту не меняет.
- `publish_order`: **article → brand → guide_route**. У `article` 24 открытые
  измеренные страницы и самая большая измеренная отдача; у `brand` спрос в очереди
  огромный, но собственной измеренной отдачи ещё нет; у `guide_route` достижимой
  строки нет вовсе, поэтому по контракту он уходит в конец.
  ⛔ Честная оговорка: спрос в очередях измерен в разных единицах (Вордстат против
  модельных кликов Wordcraft), поэтому порядок построен на измеренной отдаче сайта
  и достижимости, а не на сравнении несравнимых чисел.

### Читаемость — строка по каждому контуру

Метрика без роботов, **только органический поиск**. Прямые заходы исключены
намеренно: штатный фильтр роботов не поймал 922 захода по одной секунде из 954 на
одну статью. Окно 28 дней 05.08–01.09; динамика — два сопоставимых 14-дневных
окна августа, потому что счётчик был почти слеп до 31.07.2026.

| Контур | Визиты | Отказы | Глубина | Время | Дольше минуты | Возврат за 7 дней | Динамика |
|---|---:|---:|---:|---:|---:|---:|---|
| `article` | 5 445 | 12,8% | 1,17 | 76 с | 8,3% | 5,8% | улучшение: 67 → 88 с, отказы 13,4 → 11,8% |
| `guide_route` | 1 497 | 12,3% | 1,21 | 87 с | 12,7% | 8,2% | **ухудшение: 94 → 81 с, отказы 11,1 → 13,4%, дольше минуты 14,8 → 10,7%** |
| `news` | 111 | 18,9% | 1,13 | 42 с | 0% | 0% | одна заметка на весь контур, ряда нет |
| `brand` | 54 | 9,3% | 1,04 | 48 с | 0% | 0% | окно незрелое, ряда нет |

Порог решения по этой строке назначает Никита по первому замеру. Падение времени
или рост отказов у контура против прошлого окна называется явно и не смягчается.

## Главное наблюдение недели: провал 01.08 не закрылся

Разбор от 11.08.2026 объявил августовский провал завершённым по одному признаку —
показы вернулись. Признак оказался неполным.

При **равном объёме показов** переходы из Яндекса не вернулись:

| Период | Показов в день | Переходов в день | CTR | Средняя позиция |
|---|---:|---:|---:|---:|
| 20–31.07 (до провала) | 15 038 | 538 | 3,57% | ≈7,5 |
| 13.08–02.09 (после «возврата») | 15 387 | 326 | 2,12% | ≈8,4 |

Показов **+2%**, переходов **−39%** — около 5 900 переходов за 28 дней. На тех же
263 запросах, что были в обоих окнах, CTR упал 2,43% → 1,14%, и падение идёт не от
состава запросов: новые запросы дают CTR **выше** среднего (2,42%). Позиции просели
по ядру: «озеро рица» 5,5 → 8,5, «список вещей на море для всей семьи» 3,2 → 6,2,
«что взять на море» 6,3 → 9,0.

Что из этого следует и чего не следует:

- **Следует:** проверка, которая мерила только показы, не видела потерю переходов.
  Признак закрытия аномалии — переходы и позиция при сопоставимом объёме показов,
  а не объём показов.
- **Не следует:** причина. Перелом совпадает с вечером 31.07, когда разом менялась
  сквозная навигация и запускался раздел новостей, но совпадение по дате причиной
  не является. Откат сквозной правки пятинедельной давности без записанного
  эксперимента запрещён теми же правилами, что и объявление победителя по кликам.
- **Следующий шаг:** отдельная диагностическая задача — сравнить выдачу по трём
  просевшим запросам глазами и проверить, занял ли место нейроответ или конкурент.
  До результата ни один контур не получает `SCALE`.

## Как это влияет на запуски

1. Каждый редакционный запуск читает этот файл **до выбора темы**.
2. `publication_gate: HOLD` останавливает новую публикацию во всех контурах, но
   не мешает диагностике и исправлению причины. Локальный `HOLD` останавливает
   только соответствующий контур, если записана содержательная причина: риск
   фактов, безопасности, индексации, качества или недоступность обязательного
   доказательства. Пустая очередь не является таким основанием для `HOLD`.
3. Недельный приоритет может переставить только открытую строку профильной
   очереди. Новая тема сначала получает свежий замер и строку в очереди в том же
   PR; текст не начинается раньше.
4. `monetization_decision_gate: HOLD` не снимает существующие полезные CTA. Он
   запрещает лишь объявлять победителя, менять контрольный вариант и
   масштабировать денежную гипотезу по кликам, pending или незрелым заказам.
5. Жёсткие гейты `CLAUDE.md`, фактов, SEO, изображений, дизайна и публикации
   нельзя ослабить недельным решением.
6. Контролёр **не меняет расписания и промты автоматизаций**. Он управляет
   режимом, приоритетом и очередями через версионированный PR.
7. Если `RUN`-решение старше восьми дней, его приоритет считается устаревшим:
   исполнитель возвращается к профильной очереди и сообщает о просрочке. Явный
   `HOLD` сам не протухает и снимается только новым доказательным решением.
8. Если у разрешённого контура нет открытой измеренной строки, включается
   `QUEUE_RECOVERY`: исполнитель сверяет без выборки все кандидаты из ядра,
   последних радаров, очереди и существующих URL. Каждый кандидат получает
   запись `принят / отклонён / отложен` с датой и причиной; число найденных
   кандидатов обязано совпасть с суммой трёх исходов. Финальный `HOLD` допустим
   только после такой полной сверки и с точным условием продолжения.

## Что измеряет контролёр

Окна 28/56 дней всегда сопоставимы и заканчиваются на D−3 из-за лага GSC:

- текущие и предыдущие 28 дней;
- 56 дней для проверки устойчивости направления;
- когорты по первой дате живой публикации и типам `news`, `article`, `brand`,
  `guide_route`, без смешивания форматов;
- индексируемость, canonical, sitemap и доля проиндексированных URL;
- показы, клики, CTR и позиция в GSC и Яндекс Вебмастере;
- человеческие сессии, вовлечённость и выходы к партнёрам в Метрике, без ботов;
- покрытие точной атрибуции, зрелый `approved net revenue`, pending и reversals
  раздельно;
- читаемость по типам страниц: визиты страницей входа, отказы, глубина, среднее
  время, доля визитов дольше минуты, возвраты за 7 дней.

Решение по контуру — одно из `SCALE`, `KEEP`, `REVISE`, `HOLD`:

- `SCALE` — продолжить доказанный кластер через очередь, не повышая расписание;
- `KEEP` — оставить частоту и приоритет без причинного заявления;
- `REVISE` — поставить конкретную существующую страницу на доработку;
- `HOLD` — не публиковать, пока не выполнено записанное условие.

### Три ловушки замера, проверенные на этих данных

Каждая из них однажды уже дала неверный ответ. Проверять при каждом замере:

1. **Разрез GSC по страницам задваивает показы.** Когда Google показывает ссылки
   на разделы страницы, один показ засчитывается и странице, и каждому якорю.
   В окне 05.08–01.09 это 46 строк на 4 810 показов. Строки с `#` исключать, итог
   по сайту брать из сводки, а не из суммы разреза.
2. **Счётчик посещаемости почти слеп до 31.07.2026.** Сравнивать август с июлем
   по нему нельзя: получится «рост в восемь раз». Для длинных рядов — Вебмастер
   и Search Console.
3. **Штатный фильтр роботов Метрики ловит не всё.** На одну статью пришлось 922
   прямых захода по одной секунде из 954, и `isRobot=='No'` их пропустил.
   Читаемость считать по органическому поиску, иначе среднее время врёт вниз.

Отсутствие строки в очереди — операционный пробел, а не доказательство, что тем
нет. Контролёр не оставляет контур на `HOLD` только из-за пустой очереди: он либо
добавляет измеренного кандидата, либо назначает `QUEUE_RECOVERY` с полным
реестром покрытия и проверяемым условием следующего шага.

Одинаковое направление в двух окнах — сигнал, но не доказательство причинности.
Частоту автоматизаций контролёр не меняет. Для денежного вывода обязательны
точный join, зрелость, рубли, интервал эффекта и guardrails; для причинного
вывода об изменении — заранее записанный рандомизированный эксперимент.

## Ежемесячная техническая проверка

В первый понедельник месяца дополнительно проверить: ручные меры и безопасность
в GSC/Вебмастере, покрытие индекса, canonical, sitemap/robots, 404/redirect,
битые ссылки и полевые CWV. Красный системный дефект ставит
`publication_gate: HOLD` только когда новая публикация небезопасна или заведомо
не индексируется. Потеря денежной телеметрии ставит на `HOLD` денежное решение,
а не полезный редакционный контент.

Ближайшая — **07.09.2026**. Открытые технические долги на 04.09.2026:

- Яндекс Вебмастер держит `ERRORS_IN_SITEMAPS` (POSSIBLE_PROBLEM) на
  `sitemap-images.xml`: 2 ошибки на 95 адресов. XML валиден, все 98 адресов
  отвечают 200 без редиректов, дублей `loc` нет — какие именно две записи
  отклонены, API не расшифровывает. Требует просмотра интерфейса.
- В Google Search Console числятся две мёртвые карты сайта (`/sitemap.xml` по
  https и http), обе ведут на 404, последняя загрузка — декабрь 2025 и май 2025.
  Ключ доступа выдан только на чтение (`webmasters.readonly`), поэтому снять их
  автоматически нельзя: это ручное действие Никиты в интерфейсе.
