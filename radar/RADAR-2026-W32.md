# 📡 Quality Radar W32-2026

Еженедельный интерактивный аудит прода traveltribe.ru: живой Playwright @360/@1280
+ канон-критик + консистентность чисел. Анти-скоуп (SEO-техника/CWV/пиксели/контент-
инварианты) — см. `radar/SKILL.md`. Только отчёт, код не менялся.

Main изменился очень существенно с W31 (76+ коммитов): PR #122 закрыл 8 из 9 открытых
находок Radar W31 одним заходом; масштабная монетизационная правка ссылок на отели
(#158, 953 ссылки вели на пустую форму вместо страны); раздел `/novosti/` с ежедневной
автопубликацией (#133 и последующие); 5 новых постов (Патагония, Амазония-Эквадор,
неделя в России/Абхазии, Черногория-виза, загранпаспорт в Абхазию); правки фактуры по
визам десятка стран. Дедуп не сработал — крауливал полностью.

## Топ находок

Находок 🔴 P1 / 🟠 P2 на этой неделе нет.

**🟡 P3 — осталась открытой (частичный фикс, регрессия на своём же брейкпоинте):**
- `calculator-touch-target-40px` — PR #122 добавил `.tc-seg-btn{min-height:44px}` в
  базовое правило (`calculator.astro:967`), но не тронул mobile-override в
  `@media(max-width:640px)` (`calculator.astro:1609`), который явно задаёт
  `min-height:40px` и побеждает в каскаде именно на той ширине, где и была находка.
  Подтверждено живьём @360px: все 5 кнопок (Эконом/Комфорт/Люкс, ₽/$) — 40px.
  Фикс одной строкой: `min-height: 40px` → `44px` на `calculator.astro:1609` (или
  удалить строку — базовое правило и так даст 44px).

**🟡 P3 — закрыты, подтверждено живьём (PR #122, 29.07.2026):**
- `currency-nbsp-wrap-countries` — nbsp перед ₽ на всех 74 карточках `/countries/`.
- `seasons-mobile-sheet-radius-16px` — `.cell-panel` 16px → 12px.
- `seasons-panel-cta-brand-anchor` — кнопка панели теперь «Найти билет от N ₽»
  с реальной ценой, совпадающей с блоком цены рядом (проверено кликом на
  Австралия-восток/январь: «от 76 857 ₽» в обоих местах).
- `seasons-direction-count-plural-mismatch`, `visa-country-count-plural-mismatch`,
  `cards-country-count-plural-mismatch`, `llms-txt-post-count-plural-mismatch` —
  все четыре закрыты общим модулем `src/data/plural.js` (не косметика чисел, а
  корневая функция склонения, переиспользуемая во всех местах). Подтверждено
  живьём: «74 направления», «41 страна принимает» (и в FAQ-schema — «41
  направление принимает»), «42 страны», «60 статей» в llms.txt.

## Needs-review

Без изменений с W31 (проверено живьём):
- `cards-virtualcard-cta-no-erid` — CTA виртуальной карты (platipomiru.com) всё ещё
  без `erid` в URL. Решение владельца: даёт ли партнёр erid вообще.

## Проверено и чисто

- **Live-интерактив, ассерт видимости после клика (не просто DOM-наличие):**
  - `/packing/egypt/june/` и `/packing/japan/april/` — полный перебор who×duration×
    transport (3×4×3=36 на пост, 72 всего): **0 протечек** `[hidden]`+visible
    одновременно. `packing-filter-hidden-not-applied` (P1, W27) остаётся `fixed`.
  - Главная (`/`) — клик по вайб-фильтру «пляж»: после исключения служебной строки
    «показать ещё N» из подсчёта — все 5 видимых строк содержат `beach` в
    `data-vibe`, счётчик «в сезоне: 11», `aria-pressed=true`. (Первый прогон дал
    ложный `allBeach:false` из-за включения служебной строки в выборку — перепроверено
    точным селектором `tr:not(#moreRow)`, бага нет.)
  - `/compare/` — смена стран в `#cpA`/`#cpB` меняет рендер `#cpResult` (контент
    реально другой, `offsetParent!==null`).
  - `/calculator/` — переключение уровня комфорта (Эконом→Люкс) меняет итог
    57 709 ₽ → 323 566 ₽ живьём.
- **Число «38направленийСейчас» без пробела в `textContent`** (при первом греп-проходе
  по `document.body.textContent`) — перепроверено скриншотом `.fcount`: визуально
  пробел есть (`.fcount b{margin-right:.2rem}`), это CSS-отступ, а не `&nbsp;`/текст —
  артефакт метода замера (textContent схлопывает разметку), не баг. Ложное
  срабатывание, в реестр не добавлено.
- **Канон-критик на 5 новых постах** (Патагония, Амазония-Эквадор, неделя в России/
  Абхазии + скриншоты `/novosti/`): 0 декоративных эмодзи-наборов, 0 хвостовых «→»
  в исходниках, hairline-разделители, pill-теги (`border-radius` на бейджах — не
  текстовая секция, вне правила), CTA-анкоры benefit-first без имени бренда
  («Подобрать полис с треккингом в покрытии», «Посмотреть билеты Москва — Буэнос-
  Айрес»), `rel="sponsored"` на месте. `/novosti/` (новый тип страницы) — байлайн,
  cookie-баннер с равнозначными «Отклонить»/«Принять», без агрессивного оверлея.
- **Money-фикс #158 (953 ссылки на отели)** — вне основного скоупа (не интерактив/
  геометрия), но задета монетизация, поэтому спот-чек: `/vietnam/`, `/bali/`,
  `/turkey/` — ссылка «Подобрать отель …» ведёт на `ostrovok.ru/hotel/<страна>/`
  (Бали корректно сведён к Индонезии), не на пустую форму. Фикс подтверждён живьём.
- **Числа контента:** «70+ направлений» консистентно на `/`, `/about/`, `/countries/`,
  `llms.txt`; «74 направления» на `/seasons/` и `/countries/` (разные подсчёты —
  ожидаемо, разные фильтры каталога); «8 направлений» на `/blog/` — заголовок
  конкретной статьи про отдых с ребёнком, не общесайтовый счётчик, не баг.

## Не удалось сделать

Ничего — все пункты процедуры SKILL.md выполнены. Прямой `curl` к продовскому домену
не использовался (осознанное ограничение инструментов) — весь live-краул выполнен
через `node`/Playwright, сетевой доступ из `node` разрешён.
