# 📡 Quality Radar W35-2026

Еженедельный интерактивный аудит прода traveltribe.ru: живой Playwright @360/@1280
+ канон-критик + консистентность чисел. Анти-скоуп (SEO-техника/CWV/пиксели/контент-
инварианты) — см. `radar/SKILL.md`. Только отчёт, код не менялся.

Main изменился с W34 (78 коммитов): широкие таблицы — bleed на компьютере (#332) и
карточная раскладка на телефоне (#331), лента свежих заметок на страновом хабе (#330),
заголовки визовых страниц регионов России переставлены под вопрос про загранпаспорт
(#326), фильтр тем блога заменён с пилюль на текст-тогглы (#323), гейт языка сузился до
тронутой прозы (#324), блок «Что пишут те, кто был» появился в нескольких статьях,
новые статьи (ЮАР, Непал, Южная Корея), `chore: update flight prices` ×4. Дедуп не
сработал — main менялся широко, крауливал полностью, включая новые интерактивные и
табличные механизмы.

## Топ находок

**🟡 P3 — новая:**
- `seasons-panel-block-label-eyebrow-caps` — мобильная/десктопная панель ячейки
  `/seasons/` (открывается кликом по `td.cell[data-slug]`) показывает лейблы полей
  «Событие месяца», «Что важно знать», «Билет из Москвы» классом
  `.panel-block-label` (`seasons.astro:1020`): `letter-spacing: 0.1em;
  text-transform: uppercase`. Это тот же mono+uppercase+wide-tracking паттерн,
  который CLAUDE.md прямо запрещает и который Никита явно решил чистить на
  калькуляторе 2026-07-15 (finding `calculator-eyebrow-caps-labels`, PR #83,
  тоже полевые/статус-лейблы, не декор над H1) — то решение на `/seasons/` не
  распространилось. Скрин `radar/assets/seasons-panel-block-label-eyebrow-caps.png`
  (клик по ячейке Австралия-восток/январь, @390). Фикс по образцу PR #83: снять
  `text-transform: uppercase`, сузить `letter-spacing` — текст лейблов в разметке
  уже в sentence-case.

**🟡 P3 — остались открытыми (без изменений с W34):**
- `calculator-touch-target-40px` — mobile-override `@media(max-width:640px)`
  (`calculator.astro:1617`, `min-height:40px`) всё ещё побеждает базовое правило
  44px (`:975`). Живьём @360px: все 5 кнопок сегмент-контрола (Эконом/Комфорт/Люкс,
  ₽/$) — ровно 40px. Фикс одной строкой не изменился: `40px` → `44px` на `:1617`.
- `packing-direction-count-plural-mismatch` — `/packing/` по-прежнему не
  импортирует `NOUN` из `plural.js`. Живьём: заголовок раздела — «Все 74
  направлений» (нужно «74 направления»), счётчик поиска — «74 стран» (нужно
  «74 страны»). Рендер не менялся, скрин с W33 актуален.

## Needs-review

Без изменений с W34 (проверено живьём):
- `cards-virtualcard-cta-no-erid` — CTA виртуальной карты (platipomiru.com) всё ещё
  без `erid` в URL (`rel="sponsored noopener"` на месте). Решение владельца: даёт ли
  партнёр erid вообще.

## Проверено и чисто

- **Широкие таблицы (#331/#332, новое с W34)** — `/blog/amazonia-ecuador-2026/` и
  `/blog/kareliya-guide-2026/` (по таблице каждая): @1280 нет горизонтального
  переполнения страницы (`document.documentElement.scrollWidth ===
  clientWidth` на 1280/402/390), bleed-таблица шире контейнера текста (1055px
  против 720px родителя) не вылезает за вьюпорт; @390/@402 карточная раскладка —
  первая колонка не сжата (118–169px, не повторяет баг «144px в три строки» из
  описания PR), заголовки карточек в одну строку.
- **Лента «Свежее о {стране}» на страновом хабе (#330, новое)** — `/turkey/`
  @1280: блок `[data-volatile]` рендерится живьём («Анталья: саммит ООН в ноябре
  займёт отели Белека и Лары», дата, ссылка «Все новости путешествий»), 0 эмодзи,
  0 хвостовой «→» в тексте блока.
- **Фильтр тем блога (#323, новое)** — `/blog/` @360: toggle «Ещё 17»/«Ещё 19»
  (`<details>`/`<summary>`, без JS) — хит-зона `52×44px` (граница нормы, не ниже
  44), клик раскрывает 17 скрытых тегов, все реально видимые (`offsetParent`)
  после раскрытия. @1280: `border-radius: 0px` на всех тег-элементах (текст-тоггл,
  не пилюля — соответствует канону §5/§4 брендбука, упомянутому в самом PR).
- **Визовые страницы регионов России (#326, новое)** — `/visa/kamchatka/`,
  `/visa/dagestan/`, `/visa/altai/`, `/visa/karelia/`: `title`/`h1`/meta-description
  живьём совпадают с `visa-details.js` (переставлены на вопрос про загранпаспорт),
  друг другу не противоречат.
- **Live-интерактив, ассерт видимости после клика (не просто DOM-наличие):**
  - `/` (главная) — клик по бюджет-фильтру «до 100к»: счётчик 38→8, из 8
    совпадающих реально видимы 5 (`CAP=5` в клиентском скрипте) + строка
    «Ещё 3 в сезоне — показать все» с рабочей ссылкой на `/countries/` — это
    осознанная пагинация (`index.astro:458`), не расхождение счётчика с рендером;
    первая проверка ошибочно приняла разницу 8 vs 5 за баг, перепроверка кода
    и `data-budget` по всем 75 строкам подтвердила: капа + more-row работают как
    задумано.
  - `/seasons/` — клик по ячейке (Австралия-восток/январь) открывает `#cellPanel`:
    `getBoundingClientRect` даёt реальные `width/height` в вьюпорте, `opacity:1`,
    `visibility:visible`, `transform` съехал в `matrix(1,0,0,1,0,0)` — подтверждено
    скриншотом. Border-radius bottom-sheet 12px (`seasons-mobile-sheet-radius-16px`,
    W27, остаётся `fixed`).
  - `/packing/egypt/june/` — клик по chip-фильтру длительности («Неделя»):
    0 элементов одновременно `[hidden]` и visually-visible после клика
    (`packing-filter-hidden-not-applied`, P1 W27, остаётся `fixed`).
- **Консистентность чисел**: `DIRECTIONS.length` = 74 в исходных данных — совпадает
  с «74 направления» на `/seasons/` (там склонение верное через `NOUN`, W29-фикс
  держится); расхождение только у `/packing/` (см. открытую находку выше).

## Не удалось сделать

- Временные скрипты проверки (`radar/tmp-audit*.mjs`, `radar/tmp-overflow.mjs`,
  `radar/tmp-shot.mjs` и их JSON/PNG-вывод) не получилось удалить — `rm` внутри
  `radar/` заблокирован в этой сессии политикой безопасности харнесса, тот же
  класс ограничения, что на W33/W34. Файлы не добавлены в git (закоммичены
  только `findings.json`, новый отчёт и `radar/assets/
  seasons-panel-block-label-eyebrow-caps.png`) — физически остались в рабочей
  копии раннера, следующий прогон/очистка раннера их уберёт.
- `curl` к прод-домену не использовался (осознанное ограничение) — весь
  live-краул выполнен через Playwright/Node, которым сетевой доступ разрешён.
