---
name: design-canon
description: "Дизайн-канон traveltribe.ru: 12 обязательных стандартов (брендбук Golos и hairlines, web design 12-8-4, SEO, UX, «не похоже на AI», комплаенс, достоверность, перф-бюджет, WCAG 2.2 AA, definition of done), что использовать и анти-паттерны вёрстки, quality gate программного шаблона, сетка и адаптив 2026, UX-каноны вовлечённости. Загружать ДО правки вёрстки, стилей, компонентов, лейаутов, новых шаблонов страниц и при ревью визуала."
---

# Design canon (кодифицирован 2026-05-20 после провалов с pillowy-FAQ и emoji-tips)

Перенесено из корневого `CLAUDE.md` 08.09.2026 без изменения текста (только пути от корня репозитория). Правила действуют по умолчанию; отступление — только с явного «да» Никиты.

## 🔒 ОБЯЗАТЕЛЬНЫЕ СТАНДАРТЫ — без отступлений без явного разрешения Никиты
Все стандарты ниже применяются к КАЖДОЙ странице/компоненту **по умолчанию, не спрашивая разрешения
их применять**. Любое ОТСТУПЛЕНИЕ от любого — только с явного «да» Никиты. Утверждено 2026-05-22.

0. **ФОРМАТ СТРАНИЦЫ РАЗДЕЛА** → [`research/section-page-format.md`](research/section-page-format.md)
   — 20 источников с замерами (NN/g, Baymard, GOV.UK, W3C, web.dev, WebAIM, MDN,
   Smashing, A List Apart, айтрекинг в туризме), только исследования, не
   маркетинг. Читать ПЕРЕД правкой любого раздела. Ключевые числа: 57% времени
   на первом экране; уходы 67–90% при слабом списке против 17–33%; 38% сайтов не
   дают фильтра по данным из своей же карточки; прочтут пятую часть слов; строка
   45–85 знаков; цели ≥24 px; низкий контраст на 79% страниц мира.
   Приметы машинного текста и вида — [`research/ai-signs-checklist.md`](research/ai-signs-checklist.md),
   62 критерия со статусом «гейт / можно / глазами».

1. **БРЕНДБУК (дизайн)** → [`BRANDBOOK.md`](BRANDBOOK.md) — Swiss/International Typographic, выведен
   из главной v1. Один гротеск **Golos Text** (кириллица-натив; шрифт ОБЯЗАН поддерживать кириллицу —
   основной язык русский); палитра (`--gold #8e6618` — единственный акцент, нижняя граница
   AA); только hairlines (0 скруглений/теней/градиентов/эмодзи/второго шрифта). Полный чёрный список — в брендбуке. [[feedback_brandbook_binding]]
2. **WEB DESIGN** → канон-сетка 12-8-4 ≤1280 + практики 2026 (INP<200ms, LCP/CLS, современный CSS,
   тач ≥44, fluid+clamp); измеримая проверка @360/@1280 в 3 движках. Разделы ниже + [[feedback_webdesign_grid_2026]] · [[feedback_webdesign_practices_2026]].
3. **SEO** → [`SEO-CHECKLIST-2026.md`](SEO-CHECKLIST-2026.md) + «🚦 ОБЯЗАТЕЛЬНЫЙ гейт перед коммитом»
   в корневом `CLAUDE.md`; планка 10/10 Google+Яндекс (title-ключ, schema/JSON-LD, E-E-A-T/byline, canonical, перелинковка). [[feedback_seo_quality_bar]] · [[feedback_seo_audit_paths_not_counts]].
   Дельта к чеклисту по первоисточникам (36 открытых страниц Google, Яндекса, schema.org,
   web.dev, SISTRIX, Pew, Seer; 31 правило со ссылкой и числом, плюс раздел «что не
   подтвердилось») — [`research/seo-sources.md`](research/seo-sources.md), собрано 28.08.2026.
   Семантика и порог отбора тем — [`research/wordcraft.md`](research/wordcraft.md).
4. **UX (вовлечённость)** → страница = система принятия решения: главный интерактив в hero, guided-фильтры,
   сезонная релевантность, trust выше сгиба, first-person CTA, минимализм>плотности. [[feedback_ux_engagement_2026]] + монетизация [[feedback_monetization_ctr]].
5. **⛔ НЕ ПОХОЖЕ НА AI** (визуал И текст) → Визуал: чёрный список брендбука (0 эмодзи-декора, скруглений,
   теней, градиентов, generic cards-grid, фиолетового-на-белом); дизайн без референса = промах [[feedback_design_needs_reference]].
   Текст: человеческий голос от первого лица, без AI-slop/воды/«итак»/«в заключение», без Runglish [[feedback_no_runglish]],
   RU-предлоги и типографика (на Бали; неразрывные диапазоны). [[feedback_no_ai_look]] · [[feedback_blog_voice_quality]].
6. **ЮР-КОМПЛАЕНС РФ** → 38-ФЗ: `erid`+`rel="sponsored"` на КАЖДОЙ партнёрской ссылке (erid = юр-метка; видимой надписи «реклама» НЕ ставить — решение Никиты 2026-06)
   [[feedback_erid_required]]. 152-ФЗ: минимизация перс. данных, российские/self-hosted где важно.
   **Обязательные юр-страницы** (ссылки в футере): Политика конфиденциальности, **Cookie-политика с поимённым
   списком cookie и целью**, Пользовательское соглашение/оферта; **баннер cookie-согласия** до загрузки
   необлигаторной аналитики. Для global-аудитории учитывать EU EAA (с 06.2025) / GDPR.
7. **ДОСТОВЕРНОСТЬ / YMYL** → не выдумывать визы/цены/даты/правила; каждый YMYL-факт = первоисточник + дата +
   дисклеймер актуальности; «не знаю»/спросить вместо догадки; инференцию помечать. [[feedback_no_fabrication]].
   **Каденс ревизии:** визы/цены перепроверять по графику (визы — ≤90 дней, перед сезоном), у YMYL-страниц —
   владелец и механизм быстрой правки; при изменении факта — grep по репо + llms.txt в той же сессии.
8. **ПЕРФ-БЮДЖЕТ (блокеры)** → LCP<2.5 / INP<200 / CLS<0.1; **только webp, НИКОГДА avif** [[feedback_safari_avif_bug]];
   subset-шрифты, минимум JS, `width/height` на медиа, hero eager+fetchpriority. [[feedback_lcp_measure_first]].
9. **ДОСТУПНОСТЬ WCAG 2.2 AA** → Lighthouse A11y=100; клавиатура; `:focus-visible`; контраст ≥4.5; alt; один H1; `lang=ru`.
10. **БЕЗОПАСНОСТЬ / СЕКРЕТЫ** → действует в каждой сессии, не только при вёрстке; полный текст — в корневом `CLAUDE.md`, раздел «Design canon → навык».
11. **DEFINITION OF DONE** → измерять, не угадывать; «build ≠ работает»; визуал-гейт зелёный ДО пуша;
    Chain-of-Verification (написать опровергающие проверки и прогнать независимо). **Сверять РЕНДЕР глазами
    с одобренным эталоном** (не только метрики); проверять реально отрисованный глиф/шрифт, а не
    `computed font-family`; визуал-гейт НЕ самореференсный (не благословлять свою же вёрстку как эталон без
    сравнения с утверждённым макетом). Урок 2026-05-22: Lighthouse 100 ≠ выглядит правильно. [[feedback_definition_of_done]] · [[feedback_visual_check]].
12. **GIT / ДЕПЛОЙ** → действует в каждой сессии, не только при вёрстке; полный текст — в корневом `CLAUDE.md`, раздел «Design canon → навык».

При конфликте: визуал — приоритет **BRANDBOOK.md**; контент/разметка/перформанс — соответствующий канон.
Разделы editorial-канона ниже — легаси текущего сайта (Newsreader+Onest+warm-gold); действуют для
существующих страниц до миграции на брендбук (отдельная задача).

Программные шаблоны (`/packing/`, `/trips/[m]/[c]/`, `/[slug]/`, новые фичи) идут через **editorial-канон**, а не реюз blog-accordion стилей.

## Использовать
- **Hairline-grid** для tips/советов/чек-листов → класс `.editorial-grid` или локальный analog (`pack-list`, `tc-tips-list`).
- **Gold accent-left CTA** для cross-links → `.editorial-cta` (background rgba(gold, 0.05), border-left 3px gold).
- **Hairline FAQ** (НЕ реюз blog `.faq-item`) → `.editorial-faq` или локальный `pack-faq-*`/`tc-faq-*`.
- **Gradient+emoji hero** → `.editorial-hero` (LCP <1 с, CLS 0, использует `d.visual.g1/g2`).
- **scroll-margin-top: 80px** на ВСЕХ H2 в программных секциях (фикс перекрытия sticky-nav).
- **Локальные классы** (`pack-*`, `tc-*`, `qh-*` префикс по странице) вместо реюза global классов — изолирует от наследования.

## НЕ использовать (anti-patterns, проверены в кровь)
- ❌ `.faq-item` / `.faq-block` global на программных страницах (pillowy bg+border-radius для blog).
- ❌ `border-radius > 12px` на текстовых секциях (FAQ, tips, lists) — UI-kit-look, не editorial.
- ❌ Mixed emoji-icons (📅🛡📱👨‍👩‍👧📊🛂💳) как декор-набор — разностилевые, шум. Либо все lucide-line, либо без декор-иконок.
- ❌ Generic background-filled cards-grid 4×2 / 3×3 с shadow на hover — bootstrap-look.
- ❌ Картинки в hero программных страниц — LCP риск. Gradient+emoji = LCP <1 с.
- ❌ Hard-coded ширина >1920w для hero-cover — bandwidth waste на mobile.
- ❌ `formats={['avif']}` в Picture — Safari+astro-compress = broken image (`feedback_safari_avif_bug`).
- ❌ `align-items:baseline` во flex header-ряду «заголовок + ссылка/кнопка с `min-height`/`<input>`» — WebKit/Safari считает baseline иначе → разъезд по вертикали (кейс «Свежие истории»). Использовать `align-items:center`; baseline только для инлайн-текста одной природы (число+подпись, цена+единица). Проверять в WebKit, не только Chromium. ([[reference_spacing_alignment_canon]])
- ❌ Удвоенный вертикальный ритм: `padding` сверху И снизу у каждой стыкующейся секции → дыры (4rem+4rem=8rem). Отступ только с одной стороны + `:last-of-type{padding-bottom}`. Шкала 8pt (4/8/16/24/32/48/64); секции 48-64px десктоп / 32-40 мобайл; близость: внешний зазор > внутреннего. ([[reference_spacing_alignment_canon]])

## Quality gate
Перед коммитом нового программного шаблона:
1. Все H2 имеют `scroll-margin-top` (через editorial-* класс или локальный override).
2. Нет global-классов `.faq-item`, `.faq-block` в JSX программной страницы.
3. CTA-блоки используют `.editorial-cta` pattern (gold-left, не card-shadow).
4. Visual @402 + @1280 Playwright прогон на представителе типа.

## Сетка и адаптив 2026 (web design канон — применять на всех новых лейаутах)
Источники: UXPin UI Grids, USWDS, CMS Design System, 12-8-4. Утилиты в `global.css`, токены в `tokens.css`.
- **Сетка 12-8-4 (mobile-first):** мобайл <768px = 4 кол, планшет ≥768 = 8, десктоп ≥1024 = 12.
  Использовать `.grid-12` + спаны `.col-2/3/4/6/8`, или `.grid-auto` для карточек. Колонки fluid (`fr`),
  gutters фикс (`--gutter` 16px / `--gutter-wide` 24px). Контейнер ≤1280 (`--container-wide` 1200).
- **Таблицы на мобайле → карточки**, НЕ горизонтальный скролл (повтор десктоп-таблицы = анти-паттерн).
- **Тач-таргеты ≥44px** (Apple HIG; WCAG 2.5.8 минимум 24). Типографика — `clamp()`, не фикс-px.
- **CWV (фактор ранжирования + −24% bounce):** INP <200ms — главная боль 2026 (минимум JS, defer
  некритичного, мгновенный visual feedback на тап); LCP <2.5s (preload hero+шрифты, critical CSS);
  CLS 0 (width/height на media, fixed `aspect-ratio`, резерв под динамику).
- **Современный CSS (production 2026, заменяет JS):** container queries (компонент адаптивен к контейнеру),
  `:has()`, subgrid (выравнивание карточек), `clamp()`, `color-mix()`, `@layer`, nesting, view transitions.
- **Доступность — guardrail:** понятный value-prop и одно главное действие не конкурируют с навигацией;
  формы — минимум полей, контраст/heading-структура/keyboard-nav. Affiliate CTA над сгибом не обязателен
  и проверяется как гипотеза по стандарту `MONETIZATION-2026.md`.
- **Проверять измеримо** (overflow=0, тач ≥44px) в Chromium+WebKit+Firefox @360/390 + десктоп — не на глаз.

## UX-каноны вовлечённости 2026 (применять на КАЖДОЙ странице)
Источники задают ограничения качества; численный эффект на TravelTribe доказывает только локальный эксперимент.
Цель — страница как **система принятия решения**, а не список ссылок.
- **Главный интерактив — в центр главного контента.** Частая ошибка travel-сайтов (Baymard): поиск/подбор спрятан. На traveltribe основное действие страницы (подбор направления/поиск/фильтр) — в hero, не ниже сгиба.
- **Above-the-fold за 3-5 сек:** один ясный value-prop + одно очевидное действие по задаче страницы.
  Текст описывает результат: «Найти билет», «Подобрать направление», а не только имя сервиса.
- **Guided discovery / фасетные фильтры** (когда·месяц / бюджет / виза / вайб) проверять на
  завершении задачи, скорости и доступности; сам факт интерактивности не считается ростом вовлечённости.
- **Сезонная/персональная релевантность** — гипотеза, а не гарантированный прирост: контекст
  пользователя не выдумывается и не подменяет полный ответ.
- **Trust / social proof выше сгиба** (E-E-A-T, снижает риск): «был лично», реальные цифры из чеков, автор. Парно с [[feedback_blog_voice_quality]].
- **Эмоция + практика в правильном порядке:** фото/история будит wanderlust → практика (цены/визы/сезон) снимает неопределённость → CTA. Не просить commit до снятия неопределённости.
- **Минимализм и скорость — guardrails:** интерфейс не перекрывает текст, LCP/INP/CLS держатся
  в заданных выше пределах. Конверсионные множители без локального теста не использовать.
- Проверять измеримо, а денежный результат — по `MONETIZATION-2026.md`.
