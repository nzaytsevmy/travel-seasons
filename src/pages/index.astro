---
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';
import Layout from '../layouts/Layout.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import authorPhoto from '../assets/author/nikita.jpg';

// ── Schema ───────────────────────────────────────────
const ldJson = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebSite",
      "@id": "https://traveltribe.ru/#website",
      "name": "TravelTribe",
      "url": "https://traveltribe.ru/",
      "description": "Авторский тревел-блог из России: личные отчёты, визы, сезоны, бюджет 39 направлений мира",
      "inLanguage": "ru-RU",
      "publisher": { "@id": "https://traveltribe.ru/#org" },
      "potentialAction": {
        "@type": "SearchAction",
        "target": { "@type": "EntryPoint", "urlTemplate": "https://traveltribe.ru/?s={search_term_string}" },
        "query-input": "required name=search_term_string"
      }
    },
    {
      "@type": "Organization",
      "@id": "https://traveltribe.ru/#org",
      "name": "TravelTribe",
      "url": "https://traveltribe.ru/",
      "logo": "https://traveltribe.ru/brand-mark.svg",
      "founder": { "@type": "Person", "name": "Никита Зайцев", "url": "https://t.me/traveltriberu" },
      "sameAs": ["https://t.me/traveltriberu", "https://github.com/nzaytsevmy/travel-seasons"]
    }
  ]
};

// ── Posts ─────────────────────────────────────────
const allPosts = (await getCollection('blog')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
const featured = allPosts[0];
const restPosts = allPosts.slice(1, 7);

function readingTime(body: string) {
  return Math.max(1, Math.ceil(body.trim().split(/\s+/).length / 200));
}

// ── Categories ─── 6 главных тематических направлений
const CATEGORIES = [
  { label: 'Антарктида',   href: '/antarctica/',           emoji: '🐧', accent: 'cool' },
  { label: 'Япония',       href: '/japan/',                emoji: '🍁', accent: 'warm' },
  { label: 'Виза',         href: '/blog/tag/Виза/',        emoji: '🛂', accent: 'sand' },
  { label: 'Сезоны',       href: '/seasons/',              emoji: '🗓',  accent: 'sand' },
  { label: 'Бюджет',       href: '/calculator/',           emoji: '💸', accent: 'warm' },
  { label: 'Календарь',    href: '/trips/',                emoji: '🌍', accent: 'cool' },
];

const MONTHS_RU = ['январе','феврале','марте','апреле','мае','июне','июле','августе','сентябре','октябре','ноябре','декабре'];
const currentMonthRu = MONTHS_RU[new Date().getMonth()];
---
<Layout
  title="TravelTribe — авторский тревел-блог из России"
  description="Личные отчёты Никиты Зайцева о путешествиях. Визы, сезоны, реальные бюджеты для 39 направлений мира — Антарктида, Япония, Бали, Африка, Латам."
  canonical="https://traveltribe.ru/"
  ogTitle="TravelTribe — авторский тревел-блог из России"
  ogDescription="60+ стран, 7 континентов. Личные отчёты, реальные цены 2026, без воды и рекламы."
  ldJson={ldJson}
>
  <Nav active="home" />

  <main id="main">
    {/* ─── HERO with featured story ─── */}
    {featured && (
      <section class="hero">
        <div class="hero-image">
          <Image
            src={featured.data.coverImage}
            alt={featured.data.title}
            widths={[640, 1024, 1600, 2200]}
            sizes="100vw"
            formats={['avif', 'webp']}
            fallbackFormat="jpg"
            quality={72}
            loading="eager"
            fetchpriority="high"
            style={`object-position: ${featured.data.coverPosition || 'center'};`}
          />
          <div class="hero-overlay"></div>
        </div>
        <div class="hero-content">
          <a class="hero-link" href={`/blog/${featured.slug}/`}>
            <div class="hero-meta">
              <span class="hero-tag">// Свежее · {featured.data.tags?.[0] || 'История'}</span>
              {featured.data.sourceType === 'personal' && featured.data.tripPlace && (
                <span class="hero-trip">✓ Был лично — {featured.data.tripPlace}</span>
              )}
            </div>
            <h1 class="hero-title">{featured.data.title}</h1>
            <p class="hero-desc">{featured.data.description}</p>
            <div class="hero-cta">
              <span>Читать историю</span>
              <span class="hero-arrow">→</span>
            </div>
          </a>
        </div>
      </section>
    )}

    {/* ─── Categories ─── */}
    <section class="cat-section">
      <div class="cat-container">
        <div class="section-eyebrow">Куда сейчас ехать в {currentMonthRu}</div>
        <div class="cat-grid">
          {CATEGORIES.map((c) => (
            <a href={c.href} class={`cat-card cat-card--${c.accent}`}>
              <span class="cat-emoji" aria-hidden="true">{c.emoji}</span>
              <span class="cat-label">{c.label}</span>
              <span class="cat-arrow" aria-hidden="true">→</span>
            </a>
          ))}
        </div>
      </div>
    </section>

    {/* ─── Latest posts grid ─── */}
    {restPosts.length > 0 && (
      <section class="latest-section">
        <div class="latest-container">
          <header class="latest-header">
            <div class="section-eyebrow">// Последние истории</div>
            <h2 class="section-title">Свежие посты</h2>
            <a href="/blog/" class="latest-all">Все статьи →</a>
          </header>
          <div class="latest-grid">
            {restPosts.map((post) => (
              <a href={`/blog/${post.slug}/`} class="latest-card">
                <div class="latest-cover">
                  <Image
                    src={post.data.coverImage}
                    alt={post.data.title}
                    widths={[400, 720]}
                    sizes="(max-width: 700px) 100vw, 33vw"
                    formats={['avif', 'webp']}
                    quality={68}
                    loading="lazy"
                    style={`object-position: ${post.data.coverPositionCard || post.data.coverPosition || 'center'};`}
                  />
                </div>
                <div class="latest-body">
                  <div class="latest-tags">
                    {post.data.tags?.slice(0, 2).map((t) => (
                      <span class="latest-tag">{t}</span>
                    ))}
                  </div>
                  <h3 class="latest-title">{post.data.title}</h3>
                  <p class="latest-desc">{post.data.description}</p>
                  <div class="latest-meta">
                    <span>{readingTime(post.body)} мин</span>
                    {post.data.sourceType === 'personal' && <span class="latest-personal">· Был лично</span>}
                  </div>
                </div>
              </a>
            ))}
          </div>
        </div>
      </section>
    )}

    {/* ─── Tools strip — calc + seasons + visa-map ─── */}
    <section class="tools-section">
      <div class="tools-container">
        <div class="section-eyebrow">// Инструменты</div>
        <div class="tools-grid">
          <a href="/seasons/" class="tool-card">
            <span class="tool-icon">🗓</span>
            <strong>Таблица сезонов</strong>
            <span>39 направлений × 12 месяцев. Когда лучше ехать.</span>
          </a>
          <a href="/calculator/" class="tool-card">
            <span class="tool-icon">💸</span>
            <strong>Калькулятор бюджета</strong>
            <span>Перелёт + проживание + еда. Живой курс ЦБ.</span>
          </a>
          <a href="/trips/" class="tool-card">
            <span class="tool-icon">🌍</span>
            <strong>Календарь поездок</strong>
            <span>Что работает в каждом месяце 2026.</span>
          </a>
          <a href="/blog/" class="tool-card">
            <span class="tool-icon">📓</span>
            <strong>Блог</strong>
            <span>Личные отчёты автора. {allPosts.length} статей.</span>
          </a>
        </div>
      </div>
    </section>

    {/* ─── Newsletter signup ─── */}
    <section class="news-section">
      <div class="news-container">
        <div class="news-text">
          <div class="section-eyebrow">// Telegram-канал</div>
          <h2>@traveltriberu — личные истории и оперативные обновления</h2>
          <p>Никита пишет про текущие поездки, апдейты по визам и неочевидные открытия. Без воды и рекламы. Раз в 2-3 дня.</p>
        </div>
        <a class="news-cta" href="https://t.me/traveltriberu" target="_blank" rel="noopener">
          <span>Подписаться в Telegram</span>
          <span class="news-arrow">→</span>
        </a>
      </div>
    </section>

    {/* ─── Author strip ─── */}
    <section class="author-section">
      <a href="/about/" class="author-strip">
        <Image
          src={authorPhoto}
          alt="Никита Зайцев — автор TravelTribe"
          width={88}
          height={88}
          format="webp"
          quality={82}
          loading="lazy"
          class="author-photo"
        />
        <div class="author-text">
          <strong>Никита Зайцев</strong>
          <span>7 континентов · 60+ стран · 13 лет в продуктах · авторский блог без рекламы</span>
        </div>
        <span class="author-arrow">→</span>
      </a>
    </section>
  </main>

  <Footer />
</Layout>

<style>
/* ─── HERO — full-bleed cover photo + overlay text ─── */
.hero {
  position: relative;
  min-height: clamp(480px, 78vh, 760px);
  margin-top: -64px;  /* slip under sticky nav */
  display: grid;
  align-items: end;
  isolation: isolate;
  overflow: hidden;
}
.hero-image { position: absolute; inset: 0; z-index: -1; }
.hero-image picture, .hero-image img {
  width: 100%; height: 100%; object-fit: cover; display: block;
}
.hero-overlay {
  position: absolute; inset: 0;
  background:
    linear-gradient(to top, rgba(0,0,0,0.78) 0%, rgba(0,0,0,0.35) 50%, rgba(0,0,0,0.15) 100%),
    linear-gradient(to right, rgba(0,0,0,0.45) 0%, rgba(0,0,0,0) 60%);
  z-index: 1;
}
.hero-content {
  position: relative; z-index: 2;
  max-width: 760px; margin: 0 auto;
  padding: 0 var(--container-edge, 2rem) var(--space-12, 3rem);
  width: 100%;
}
.hero-link {
  display: block; color: #fff; text-decoration: none;
  transition: transform var(--duration-base) var(--ease-out);
}
.hero-link:hover { transform: translateY(-2px); }
.hero-link:hover .hero-arrow { transform: translateX(4px); }
.hero-meta {
  display: flex; flex-wrap: wrap; align-items: center; gap: 1rem;
  margin-bottom: var(--space-4, 1rem);
}
.hero-tag {
  font-size: var(--text-sm, 0.875rem); letter-spacing: var(--tracking-widest);
  text-transform: uppercase; color: var(--gold-300, #e8c062);
}
.hero-trip {
  font-size: var(--text-sm); color: rgba(255,255,255,0.85);
  border: 1px solid rgba(232,192,98,0.4);
  background: rgba(232,192,98,0.12);
  padding: 0.25rem 0.7rem; border-radius: var(--radius-pill);
}
.hero-title {
  font-family: var(--ff-display);
  font-size: var(--text-7xl);
  font-weight: 700;
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-tight);
  color: #fff;
  text-shadow: 0 2px 16px rgba(0,0,0,0.4);
  margin: 0 0 var(--space-4, 1rem);
  text-wrap: balance;
}
.hero-desc {
  font-size: var(--text-lg); line-height: var(--leading-relaxed);
  color: rgba(255,255,255,0.9); max-width: 560px;
  margin: 0 0 var(--space-6, 1.5rem);
}
.hero-cta {
  display: inline-flex; align-items: center; gap: 0.5rem;
  font-size: var(--text-base); font-weight: 600; letter-spacing: var(--tracking-wide);
  color: #fff; padding: 0.85rem 1.5rem;
  background: var(--gold-600, #c9973a);
  border-radius: var(--radius-pill);
  box-shadow: var(--shadow-md);
  transition: background var(--duration-fast), box-shadow var(--duration-base);
}
.hero-cta:hover { background: var(--gold-500); box-shadow: var(--shadow-lg); }
.hero-arrow {
  display: inline-block;
  transition: transform var(--duration-base) var(--ease-out);
}

/* ─── SECTION primitives ─── */
.section-eyebrow {
  font-size: var(--text-xs); letter-spacing: var(--tracking-widest);
  text-transform: uppercase; color: var(--gold-600);
  margin-bottom: var(--space-3);
}
.section-title {
  font-family: var(--ff-display);
  font-size: var(--text-4xl);
  font-weight: 600; line-height: var(--leading-tight);
  letter-spacing: var(--tracking-tight);
  margin: 0;
}

/* ─── CATEGORIES ─── */
.cat-section { padding: var(--space-12) 0 var(--space-6); }
.cat-container { max-width: var(--container-wide, 1200px); margin: 0 auto; padding: 0 var(--container-edge, 2rem); }
.cat-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: var(--space-3);
  margin-top: var(--space-4);
}
.cat-card {
  display: inline-flex; align-items: center; gap: var(--space-3);
  padding: var(--space-4) var(--space-5);
  background: var(--bg2);
  border: 1px solid var(--line);
  border-radius: var(--radius-3, 6px);
  text-decoration: none;
  color: var(--text);
  transition: border-color var(--duration-fast), transform var(--duration-fast), background var(--duration-fast);
}
.cat-card:hover {
  border-color: rgba(201,151,58,0.45);
  transform: translateY(-1px);
  background: var(--bg3);
}
.cat-emoji { font-size: 1.4rem; line-height: 1; }
.cat-label { flex: 1; font-weight: 500; font-size: var(--text-base); }
.cat-arrow { color: var(--gold-600); font-size: 1.1rem; transition: transform var(--duration-fast); }
.cat-card:hover .cat-arrow { transform: translateX(3px); }

/* ─── LATEST POSTS ─── */
.latest-section { padding: var(--space-12) 0; }
.latest-container { max-width: var(--container-wide); margin: 0 auto; padding: 0 var(--container-edge); }
.latest-header {
  display: flex; align-items: end; justify-content: space-between; gap: var(--space-4);
  margin-bottom: var(--space-8);
  padding-bottom: var(--space-4);
  border-bottom: 1px solid var(--line);
}
.latest-all {
  font-size: var(--text-sm); letter-spacing: var(--tracking-wide);
  color: var(--gold-600); text-decoration: none;
  white-space: nowrap;
}
.latest-all:hover { color: var(--gold-500); }
.latest-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: var(--space-6);
}
.latest-card {
  display: flex; flex-direction: column;
  background: var(--bg2);
  border: 1px solid var(--line);
  border-radius: var(--radius-4, 8px);
  overflow: hidden;
  text-decoration: none;
  color: inherit;
  transition: transform var(--duration-base), box-shadow var(--duration-base), border-color var(--duration-base);
}
.latest-card:hover {
  transform: translateY(-3px);
  box-shadow: var(--shadow-lg);
  border-color: rgba(201,151,58,0.4);
}
.latest-cover {
  aspect-ratio: 16 / 10;
  overflow: hidden;
  background: var(--bg3);
}
.latest-cover img {
  width: 100%; height: 100%; object-fit: cover; display: block;
  transition: transform var(--duration-slow) var(--ease-out);
}
.latest-card:hover .latest-cover img { transform: scale(1.04); }
.latest-body { padding: var(--space-5); display: flex; flex-direction: column; gap: var(--space-2); flex: 1; }
.latest-tags { display: flex; flex-wrap: wrap; gap: 0.35rem; margin-bottom: 0.2rem; }
.latest-tag {
  font-size: var(--text-xs); letter-spacing: 0.06em; text-transform: uppercase;
  color: var(--gold-600);
  border: 1px solid rgba(201,151,58,0.3);
  padding: 0.12rem 0.5rem;
  border-radius: var(--radius-1);
}
.latest-title {
  font-family: var(--ff-display);
  font-size: var(--text-xl);
  font-weight: 600; line-height: var(--leading-snug);
  margin: 0;
  text-wrap: balance;
}
.latest-desc {
  font-size: var(--text-sm); color: var(--text-dim);
  line-height: var(--leading-normal);
  margin: 0;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
.latest-meta {
  font-size: var(--text-xs); color: var(--text-faint);
  letter-spacing: var(--tracking-wide);
  margin-top: auto;
}
.latest-personal { color: var(--gold-600); }

/* ─── TOOLS ─── */
.tools-section { padding: var(--space-8) 0 var(--space-12); }
.tools-container { max-width: var(--container-wide); margin: 0 auto; padding: 0 var(--container-edge); }
.tools-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: var(--space-3);
  margin-top: var(--space-4);
}
.tool-card {
  display: flex; flex-direction: column; gap: var(--space-2);
  padding: var(--space-5);
  background: var(--bg2);
  border: 1px solid var(--line);
  border-radius: var(--radius-3);
  text-decoration: none;
  color: var(--text);
  transition: border-color var(--duration-fast), background var(--duration-fast);
}
.tool-card:hover { border-color: rgba(201,151,58,0.4); background: var(--bg3); }
.tool-icon { font-size: 1.6rem; line-height: 1; }
.tool-card strong { font-size: var(--text-base); font-weight: 600; }
.tool-card span:last-child { font-size: var(--text-sm); color: var(--text-dim); line-height: var(--leading-normal); }

/* ─── NEWSLETTER ─── */
.news-section {
  padding: var(--space-12) 0;
  background: linear-gradient(180deg, transparent, rgba(201,151,58,0.04));
}
.news-container {
  max-width: 960px; margin: 0 auto;
  padding: var(--space-10) var(--container-edge);
  background: var(--bg2);
  border: 1px solid rgba(201,151,58,0.25);
  border-radius: var(--radius-4);
  display: flex; flex-wrap: wrap; align-items: center; justify-content: space-between; gap: var(--space-6);
  position: relative; overflow: hidden;
}
.news-container::before {
  content: ""; position: absolute; top: 0; left: 0; right: 0; height: 1px;
  background: linear-gradient(90deg, transparent, var(--gold-500), transparent);
}
.news-text { flex: 1 1 300px; }
.news-text h2 {
  font-family: var(--ff-display); font-size: var(--text-2xl);
  font-weight: 600; line-height: var(--leading-snug);
  margin: 0.4rem 0 var(--space-2);
  text-wrap: balance;
}
.news-text p { font-size: var(--text-base); color: var(--text-dim); line-height: var(--leading-relaxed); margin: 0; max-width: 480px; }
.news-cta {
  display: inline-flex; align-items: center; gap: 0.5rem;
  background: var(--gold-600); color: #fff;
  font-weight: 600; font-size: var(--text-base);
  padding: var(--space-3) var(--space-6);
  border-radius: var(--radius-pill);
  text-decoration: none;
  white-space: nowrap;
  box-shadow: var(--shadow-md);
  transition: background var(--duration-fast), transform var(--duration-fast);
}
.news-cta:hover { background: var(--gold-500); transform: translateY(-1px); }
.news-arrow { transition: transform var(--duration-fast); }
.news-cta:hover .news-arrow { transform: translateX(3px); }

/* ─── AUTHOR ─── */
.author-section { padding: 0 0 var(--space-16); }
.author-strip {
  max-width: 720px; margin: 0 auto;
  padding: var(--space-4) var(--space-5);
  display: flex; align-items: center; gap: var(--space-4);
  background: var(--bg2);
  border: 1px solid var(--line);
  border-radius: var(--radius-3);
  text-decoration: none;
  color: var(--text);
  transition: border-color var(--duration-fast), transform var(--duration-fast);
  margin-left: var(--container-edge); margin-right: var(--container-edge);
}
@media (min-width: 760px) { .author-strip { margin-left: auto; margin-right: auto; } }
.author-strip:hover { border-color: rgba(201,151,58,0.5); transform: translateY(-1px); }
.author-photo {
  width: 64px; height: 64px;
  border-radius: 50%; object-fit: cover; object-position: center 30%;
  border: 2px solid rgba(201,151,58,0.4);
  flex: 0 0 auto;
}
.author-text { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 0.15rem; }
.author-text strong { font-size: var(--text-base); font-weight: 600; }
.author-text span { font-size: var(--text-sm); color: var(--text-dim); }
.author-arrow { color: var(--gold-600); font-size: 1.2rem; transition: transform var(--duration-fast); }
.author-strip:hover .author-arrow { transform: translateX(3px); }

/* ─── Mobile adjustments ─── */
@media (max-width: 600px) {
  .hero { min-height: 60vh; }
  .hero-content { padding-bottom: var(--space-8); }
  .hero-title { font-size: clamp(2.4rem, 9vw, 3.4rem); }
  .latest-header { flex-direction: column; align-items: stretch; }
  .news-container { padding: var(--space-6); }
  .news-text h2 { font-size: var(--text-xl); }
}
</style>
